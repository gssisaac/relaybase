{
  "name": "relaybase-desktop",
  "version": "0.1.3",
  "private": true,
  "description": "Relaybase desktop shell (Tauri + Next.js dual-mode)",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "bash scripts/deploy/build-macos.sh aarch64",
    "build:macos": "bash scripts/deploy/build-macos.sh aarch64",
    "build:macos:aarch64": "bash scripts/deploy/build-macos.sh aarch64",
    "build:macos:x86_64": "bash scripts/deploy/build-macos.sh x86_64",
    "build:local": "pnpm exec tauri build --bundles app,dmg",
    "build:local:debug": "pnpm exec tauri build --debug --bundles app,dmg",
    "build:local:app": "pnpm exec tauri build --bundles app",
    "open:local:dmg": "bash -c 'open src-tauri/target/release/bundle/dmg/*.dmg'",
    "open:local:debug:dmg": "bash -c 'open src-tauri/target/debug/bundle/dmg/*.dmg'",
    "open:local:app": "open src-tauri/target/release/bundle/macos/Relaybase.app",
    "install:local": "pnpm run build:local && pnpm run open:local:dmg",
    "install:local:debug": "pnpm run build:local:debug && pnpm run open:local:debug:dmg",
    "sync:release": "RELAYBASE_MAC_ARCH=aarch64 node scripts/sync-release-artifacts.mjs",
    "sync:release:aarch64": "RELAYBASE_MAC_ARCH=aarch64 node scripts/sync-release-artifacts.mjs",
    "sync:release:x86_64": "RELAYBASE_MAC_ARCH=x86_64 node scripts/sync-release-artifacts.mjs",
    "upload:r2": "RELAYBASE_MAC_ARCH=aarch64 bash scripts/deploy/upload-release-r2.sh",
    "upload:r2:aarch64": "RELAYBASE_MAC_ARCH=aarch64 bash scripts/deploy/upload-release-r2.sh",
    "upload:r2:x86_64": "RELAYBASE_MAC_ARCH=x86_64 bash scripts/deploy/upload-release-r2.sh",
    "verify:release": "node scripts/deploy/verify-release-bundle.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
