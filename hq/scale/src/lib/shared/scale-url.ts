/** Public links in outbound mail (unsubscribe, open/click, assets). Served via relaybase.email → hq/scale. */
export const SCALE_PUBLIC_BASE_URL =
  process.env.SCALE_PUBLIC_BASE_URL ?? "https://relaybase.email";
