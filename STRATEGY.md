# Relaybase — 가격 및 시장 전략

- 작성일: 2026-08-09 (v1), 업데이트: 2026-08-09 (v2 — 가격체계 재설계, 7번 섹션)
- 전제: `docs/pivot-byo-cloudflare.md` / `PRODUCT.md`의 확정 방향 — **Mac(→Windows) 데스크톱 앱**, Relaybase는 서버를 운영하지 않고 사용자가 자신의 Cloudflare 계정에 `server/`(라우팅 Worker)를 직접 설치(`server/customer-install/` ZIP + Wrangler)한다. **가격 자체($39 1회)는 이 문서에서 검증 대상이며 확정 전제가 아니다** — 7번 섹션에서 재설계함.
- 이 문서의 목적: 데스크톱 앱을 둘러싼 세 가지 열린 가격/시장 질문(Worker 오픈소스화, 멀티 디바이스/모바일 라이선싱, 팀 공유 가격)에 대해 코드 근거 기반으로 답하고, 실행 가능한 SKU 구조를 제안한다.

> **읽는 순서 안내**: 1~6번 섹션은 "$39 일회성 + 구독 없음"을 전제로 세운 v1 초안이다. 제품이 아직 공개 전이고 이 전제 자체가 마케팅 카피의 선택이었을 뿐 지켜야 할 제약이 아니라는 판단에 따라, **7번 섹션에서 가격체계를 마케팅 카피와 독립적으로 객관적 지표 기반으로 재검토하고 새 모델(v2)을 제안**한다. 1~3번 질문에 대한 답(오픈소스 여부, 라이선스 스코프 정의, 팀 시트의 기술적 구조)은 v2에서도 그대로 유효하다 — 바뀌는 것은 가격 숫자와 "구독을 완전히 금지하는가"라는 전제뿐이다.

**결론 요약 (v1 — 구독 없음 전제, 아래 7번 섹션에서 재검토됨)**

| 질문 | 결론 |
|---|---|
| 1. Worker를 GitHub에 공개할까? | **공개한다.** 단 "완전 오픈소스"가 아니라 **소스 공개(source-available) + 상업적 재판매 금지 라이선스**로. 데스크톱 앱은 계속 비공개. (v2에서도 유효) |
| 2. 멀티 데스크톱/모바일 관리 | 라이선스 스코프를 **"디바이스 수"가 아니라 "Worker 설치(=사업체) 수"**로 재정의. 같은 사람의 Mac+Windows+모바일은 추가 과금 없이 같은 라이선스로 허용, 기기 활성화에는 완화된 소프트 캡만 둔다. (v2에서도 유효) |
| 3. 팀원에게 특정 이메일만 공유 | **구독이 아니라 "Team" 일회성 SKU + 저가 시트 애드온**. Relaybase는 서버 운영비가 0이므로(사용자 자신의 CF 계정에서 실행), Front/Missive식 월 구독형 시트 과금은 자사 최대 강점(무과금 인프라)과 모순된다. (v2에서 시트 구조는 유지, 가격 숫자만 조정) |

---

## 0. 현재 상태 요약 (근거)

- 라이선스는 이메일당 1개 키, 디바이스/기기 개념이 전혀 없다. `verifyLicense`는 키 해시가 KV에 있고 `active`이면 무조건 통과시킨다 — 몇 대에서 쓰든 제한이 없다.

```62:75:server/src/lib/licenses.ts
export async function verifyLicense(
  kv: KVNamespace,
  licenseKey: string,
): Promise<LicenseRecord | null> {
  const trimmed = licenseKey.trim();
  if (!trimmed.startsWith("rb_lic_")) return null;
  const keyHash = await sha256Hex(trimmed);
  const raw = await kv.get(keyKv(keyHash));
  if (!raw) return null;
  const stored = JSON.parse(raw) as StoredLicense;
  if (!stored.active) return null;
  const { keyHash: _h, ...record } = stored;
  return record;
}
```

- 앱과 사용자 Worker 사이의 신뢰 모델은 "Bearer `ADMIN_TOKEN`을 가진 자 = 전체 권한"이다. 도메인/이메일 단위로 권한을 쪼개는 개념이 없다 — 팀원에게 "이메일 하나만" 내줄 방법이 지금은 없다.
- 다만 `/v1/send`용 API 키는 이미 **도메인 단위 스코프**로 발급된다 — 이 패턴을 이메일(mailbox) 단위로 좁히면 3번 질문의 기술적 해법이 된다.

```1:16:server/src/lib/keys.ts
export type KeyRecord = {
  id: string;
  domain: string;
  label: string | null;
  keyPrefix: string;
  createdAt: string;
  active: boolean;
};
```

- 사용자에게 배포되는 코드는 이미 한 번 "고객용으로 살균(sanitize)"되어 있다 — `account_id`, KV id, 커스텀 도메인이 빠진 템플릿이 `server/customer-install/`에 별도로 존재한다. 이는 오픈소스화 시 그대로 공개 대상이 될 수 있는 형태다.

```10:19:server/customer-install/README.md
## 1. Create storage (exact names)

```bash
npx wrangler kv namespace create relaybase-keys
npx wrangler kv namespace create relaybase-api
npx wrangler r2 bucket create relaybase-inbound
```
```

---

## 1. Worker를 GitHub에 공개하는 것이 옳은가?

### 1.1 결론

**공개해야 한다.** 다만 "MIT 던지고 끝"이 아니라 **소스 공개 + 상업적 재판매/경쟁 서비스화 금지** 조건을 붙인 라이선스(Fair Source / BSL 계열)로 공개하고, 데스크톱 앱(`desktop/`, `app/`)과 운영자 대시보드(`admin/`)는 계속 비공개로 남긴다.

### 1.2 공개해야 하는 이유

1. **"우리는 당신의 메일을 처리하지 않는다"는 핵심 주장 자체가 코드 공개를 요구한다.** Relaybase의 포지셔닝은 "당신 계정에서 실행되는 소프트웨어"다(`PRODUCT.md`). 그런데 사용자에게 `ADMIN_TOKEN`을 발급받아 **자기 계정에** 낯선 바이너리를 `wrangler deploy`하라고 요구하면서 그 코드를 볼 수 없게 막는 것은 메시지와 정반대다. 타깃 고객(Cloudflare를 이미 쓰는 개발자)은 정확히 "배포 전에 코드부터 읽어보는" 사람들이다 — 이들에게는 클로즈드 Worker가 오히려 전환 장벽이다.
2. **인바운드 메일이라는 데이터의 성격상 신뢰가 구매 결정 요인이다.** Worker는 사용자의 실제 수신 이메일(R2에 원본 저장)과 발신 권한을 다룬다. "이 코드가 외부로 뭔가를 보내지 않는다"를 사용자가 직접 확인할 수 있어야 판매 저항이 줄어든다.
3. **Worker 자체는 기술적 모트(moat)가 아니다.** Cloudflare Email Sending/Routing API를 감싸는 라우팅 계층일 뿐이며, 실제 상품 가치는 데스크톱 앱의 UX(Spark 스타일 인박스, Compose, Domains, Audience/Broadcasts 대시보드)와 온보딩 경험(ZIP+가이드+라이선스+지원)에 있다. 이 부분은 계속 비공개다. Worker를 공개해도 "제품"을 그대로 복제하려면 데스크톱 앱을 처음부터 다시 만들어야 한다.
4. **무료 유통 채널이 된다.** Cloudflare/Workers 커뮤니티(GitHub, HN, r/CloudFlare)는 정확히 타깃 페르소나가 모이는 곳이다. 오픈소스 저장소는 SEO/입소문 효과가 있고, `website/content/resources/*` 콘텐츠 마케팅과 같은 목적(검증 가능성 + 발견 가능성)을 코드로 한 번 더 수행한다.
5. **지원 비용 절감.** 사용자가 `/health`, `/console/connect` 실패를 스스로 코드로 디버깅할 수 있고, GitHub Issues가 검색 가능한 지식베이스가 된다.

### 1.3 공개 시 실제로 문제가 되는 지점과 대응

| 우려 | 실제 심각도 | 대응 |
|---|---|---|
| 누군가 코드를 가져다 경쟁 "호스팅형" 서비스를 만든다 | 중 (법적 억지력이 필요) | MIT/Apache 대신 **재판매·호스팅 서비스화 금지** 조항이 있는 소스 공개 라이선스 채택 (아래 1.4) |
| 누군가 이 Worker만 가져다 무료로 쓰고 $39 데스크톱 앱을 안 산다 | **낮음** — 이미 지금도 가능(고객용 ZIP이 이미 공개적으로 다운로드 가능, `website/public/downloads/`). Worker만 raw HTTP API로 쓰는 사람은 UI/Compose/Inbox/Audience 대시보드가 없는 상태를 감당해야 한다. 이는 지금 구조가 이미 "공개나 다름없는" 상태라는 뜻이기도 하다. | 데스크톱 앱의 UX 완성도·업데이트·지원을 계속 유일한 유료 가치로 유지 |
| `ADMIN_TOKEN` 기반 인증 모델의 취약점이 코드로 노출된다 | 낮음 | Bearer 토큰 모델은 이미 README에 문서화되어 있어 "코드를 몰라서 안전한" 상태가 아니다(Kerckhoffs 원칙). 오히려 공개 후 외부 보안 리뷰를 받는 편이 낫다 |
| 라이선스 검증 로직(`licenses.ts`)이 노출되어 우회된다 | 낮음 | 키는 24바이트(192비트) 랜덤이고 KV 해시 조회로만 통과한다 — 알고리즘을 안다고 위조 불가능. 다만 **라이선스 서버(`/v1/license/*`, `admin/`)는 공개하지 않는다** — 이건 운영 비즈니스 로직이라 공개 이유가 없다 |
| Relaybase 자체 운영 계정(`api.relaybase.xyz`) 관련 설정 노출 | 낮음, 관리 필요 | `server/wrangler.toml`(운영용, account_id·KV id·커스텀 도메인 포함)이 아니라 이미 존재하는 `server/customer-install/`(살균된 템플릿)을 공개 저장소의 소스로 삼는다. 운영용 `server/wrangler.toml`은 비공개 배포 파이프라인에만 남긴다 |

### 1.4 무엇을, 어떤 라이선스로 공개할지

- **공개**: `server/customer-install/`을 기준으로 한 독립 저장소(예: `relaybase/relaybase-worker`). 라우팅/발신/수신/웹훅/키 로직 전부 포함.
- **비공개 유지**: `app/`(데스크톱 UI), `desktop/`(Tauri 셸), `admin/`(운영자 대시보드·라이선스 관리), 그리고 운영용 `server/wrangler.toml`/시크릿.
- **라이선스 후보**:
  - **BSL(Business Source License)** 또는 **Fair Source License(FSL)**: 소스를 전부 볼 수 있고 개인/자기 계정 용도로 자유롭게 수정·재배포 가능하지만, "Relaybase와 경쟁하는 유료 호스팅 서비스"로 상업화하는 것만 금지. n8n, Sentry, PocketBase 계열이 쓰는 패턴과 동일한 목적.
  - 라이선스 조항에 "본인 소유 Cloudflare 계정에 배포해 자신의 이메일을 처리하는 용도"는 명시적으로 항상 허용된다고 적어야 한다 — 이게 제품의 핵심 약속이기 때문.
- README 최상단에 신뢰 메시지 한 줄: *"이 Worker는 오픈소스입니다 — 우리가 아니라 당신이 직접 검증할 수 있습니다. 데스크톱 앱만 상업 제품입니다."* 이 문장 자체가 웹사이트/런칭(HN 등) 카피로 재사용 가능하다.

### 1.5 실행 체크리스트

- [ ] `server/customer-install/` 기준 별도 공개 저장소 생성, 라이선스 파일(BSL/FSL 텍스트) 추가
- [ ] 운영용 시크릿/계정 정보가 공개 저장소 히스토리에 전혀 없는지 재확인(스캐너로 1회 스캔)
- [ ] `admin/`, `app/`, `desktop/`는 명시적으로 비공개 저장소로 분리(현재 모노레포 구조라면 공개 전 서브트리 분리 필요)
- [ ] 웹사이트 신뢰 섹션(`website/src/components/cloudflare-trust.tsx`)에 "오픈소스 Worker" 배지 추가

---

## 2. 멀티 데스크톱(Windows) + 모바일 — 같은 Worker를 공유하는 사용자 관리

### 2.1 문제의 본질

지금 라이선스는 "이메일 1개 = 키 1개, 기기 제한 없음"이다. Windows 앱과 모바일 앱이 나오면 같은 사람이 Mac+Windows+iPhone에서 **자신의 것과 동일한 Worker**(같은 `workerUrl`+`adminToken`)에 접속하는 시나리오가 정상적으로 발생한다. 여기서 "기기 수"를 과금 기준으로 삼으면 정직한 1인 사용자를 처벌하고, "무제한"으로 두면 라이선스 키 공유(포럼에 키 유출 등)를 막을 방법이 없다.

### 2.2 핵심 원칙: 스코프는 "기기"가 아니라 "Worker 설치(=사업체)"

라이선스가 실제로 게이트해야 하는 것은 **몇 대의 기기를 쓰느냐**가 아니라 **몇 개의 독립된 Worker 설치(=몇 개의 사업/브랜드)를 운영하느냐**다.

- 한 사람이 Mac, Windows, 모바일에서 **동일한 하나의 Worker**(하나의 사업)를 관리 → **추가 과금 없음**. "한 번 사서 어디서나 쓴다"는 메시지 자체가 Google Workspace(seat×월)·Front/Missive(seat×월) 대비 가장 강력한 차별점이므로, 플랫폼 수로 다시 돈을 받는 것은 이 차별점을 스스로 깎는 행위다.
- 한 사람(또는 팀)이 **서로 다른 두 Worker**(예: 별도의 두 스타트업, 별도의 두 CF 계정)를 운영 → 이건 별개 라이선스 판매가 정당한 지점이다. 이미 `admin/keys`처럼 도메인 단위로 리소스를 스코프하는 관례가 있으므로, 라이선스도 "설치(Worker) 단위"로 스코프하는 것이 코드베이스의 기존 패턴과 일관적이다.

### 2.3 구현: 소프트 기기 캡 + 이상 탐지 (하드 DRM 지양)

`LicenseRecord`(`server/src/lib/licenses.ts`)에 활성화 목록을 추가한다:

```ts
type LicenseRecord = {
  // ...existing fields...
  activations: Array<{
    deviceId: string;      // OS 키체인에 저장된 랜덤 UUID, 앱 최초 실행 시 생성
    platform: "macos" | "windows" | "ios" | "android";
    workerUrlHash: string; // 연결된 Worker URL의 해시 (평문 저장 안 함)
    lastSeenAt: string;
  }>;
};
```

- **소프트 캡**(예: 5개 활성 기기): 초과 시 차단하지 말고 "이 라이선스는 5대에서 활성화되어 있습니다 — 설정에서 기기를 정리하세요" 안내 + 자가 서비스 비활성화 버튼. Sketch/Ulysses식 friendly floating activation과 동일한 톤 — 타깃 고객(구독/락인을 싫어해서 이 제품을 사는 사람들)에게 강압적 DRM은 역효과다.
- **이상 탐지 신호**: 하나의 라이선스가 서로 다른 `workerUrlHash`(= 서로 다른 사업체) 여러 개에 연결되면 이는 "정상적인 멀티 디바이스"가 아니라 "라이선스 키 공유" 신호다. `admin/` Licenses 화면에 이 신호를 노출해 운영자가 리뷰·회수 판단.
- 이렇게 하면 "같은 사업의 여러 기기"와 "여러 사람이 키를 나눠 쓰는 것"을 **Worker URL의 개수**로 구분할 수 있다 — 기기 수만으로는 이 구분이 불가능하다.

### 2.4 플랫폼별로 별도 과금해야 하는가?

- **Windows(데스크톱)**: 별도 과금하지 않는다. 같은 "데스크톱" 카테고리이고, 같은 UI/기능을 제공하는 재빌드일 뿐이다 — 여기서 다시 돈을 받으면 "OS 세금"으로 인식되어 리뷰/입소문에 악영향.
- **모바일 컴패니언 앱**: 기존 라이선스 보유자에게는 **무료로 포함**하되, 앱스토어 배포 자체는 훌륭한 무료 마케팅 채널로 활용한다 — 모바일 앱을 무료/가벼운 형태로 배포해 발견성을 높이고("이 앱을 쓰려면 Desktop $39가 필요합니다" 퍼널), 실제 매출은 여전히 최초 데스크톱 라이선스 1건에서 발생하게 한다.
- 예외적으로 엔지니어링 비용이 실제로 큰 기능(예: 모바일 전용 푸시 알림 인프라, 별도 알림 서버 등 Relaybase가 실제로 운영 비용을 지는 부분)이 생기면 그 부분만 별도 유료 애드온으로 분리하는 것은 정당하다 — 하지만 이는 "플랫폼 접근권"이 아니라 "Relaybase가 실제로 호스팅하는 기능"에 대한 과금이라는 점을 분명히 해야 한다(모델 일관성 유지).

### 2.5 추천 SKU (2번 관련)

| SKU | 가격 | 범위 |
|---|---|---|
| **Solo**(현행 $39) | $39 1회 | 1개 Worker 설치, 무제한 개인 기기(Mac/Windows/모바일), 소프트 캡 5기기 |
| **Studio**(신규) | $99 1회 (또는 3개 묶음 할인) | 3개 Worker 설치(=3개 사업체/클라이언트) 묶음 — 에이전시·멀티 프로덕트 운영자용, `PRODUCT.md`의 "멀티 프로덕트 운영자" 페르소나와 정확히 일치 |
| 추가 Worker 설치 | $29 1회/개 | Studio 이후 추가 사업체 |

---

## 3. 팀원에게 특정 이메일만 "Email 모드"로 공유 — 가격 전략

### 3.1 지금 구조로는 불가능하다 (기술 갭)

현재 신뢰 모델은 이진법이다: `ADMIN_TOKEN`을 가진 사람은 모든 도메인, 모든 메일함, 키 발급, 웹훅, 대시보드 전체에 접근한다. "billing@ 팀원 A에게는 support@ 딱 하나만, 그것도 읽기/답장만" 같은 요청은 **역할 기반 접근 제어(RBAC)가 아예 없어서** 지금은 ADMIN_TOKEN을 그대로 넘기는 것 외에 방법이 없다 — 이는 보안적으로도 받아들일 수 없다.

다행히 확장할 기존 패턴이 있다: `/v1/send` API 키는 이미 **도메인 단위 스코프**로 발급된다(`server/src/lib/keys.ts`). 이 스코프 개념을 한 단계 더 좁혀 **메일함(email) 단위 + 역할(role) 단위**로 확장하면 된다.

```ts
// keys.ts KeyRecord 확장 방향
type SeatRecord = {
  id: string;
  workerOwnerLicenseId: string;
  scope: "mailbox";
  mailbox: string;        // 예: "support@yourdomain.com" — 이 한 주소만
  role: "email-only";     // 대시보드/도메인/키 화면은 앱 UI에서 숨김
  invitedEmail: string;
  active: boolean;
};
```

앱 쪽에서는 이 `role: "email-only"` 토큰으로 로그인하면 Domains/Accounts/API Keys/Settings 등 관리 화면을 렌더링하지 않고 해당 메일함의 인박스/컴포즈만 노출하는 "제한 모드" 셀을 하나 추가하면 된다 — UI 골격(`app/src/relaybase-email/panel.tsx`)은 그대로 두고 역할에 따라 섹션만 필터링하는 정도의 작업이라 큰 리팩터는 아니다.

### 3.2 이 기능의 가격을 정할 때 반드시 고려해야 할 구조적 사실

Front, Missive, Help Scout 같은 "공유 인박스" 경쟁사들이 시트당 월 $20~50를 받는 이유는 **그들이 메일을 자기 서버에서 호스팅하기 때문에 시트가 늘수록 실제 운영비(스토리지, 처리량, 지원)가 늘어난다**. Relaybase는 이 전제가 아예 없다 — 메일은 사용자 자신의 Cloudflare 계정(R2/KV)에서 처리되고, Relaybase는 그 경로에 개입하지 않는다(`docs/pivot-byo-cloudflare.md`). **팀원이 몇 명이든 Relaybase가 지는 추가 인프라 비용은 0에 수렴한다.**

즉 "시트당 월 구독"을 붙이는 순간, Relaybase는 자신이 이기려는 바로 그 경쟁 모델(무과금 인프라 위에서 셋업비만 받는 소프트웨어)을 스스로 버리고 "우리도 결국 SaaS 시트 장사"가 되어버린다. 이는 브랜드 포지셔닝(`README.md`의 "We do not host your mail")과 정면으로 충돌한다.

### 3.3 추천: 구독이 아니라 "Team" 일회성 SKU + 저가 시트 애드온

| SKU | 가격 | 포함 |
|---|---|---|
| **Solo**(현행) | $39 1회 | 관리자 1명, 팀 시트 기능 없음(대상: 1인 빌더) |
| **Team**(신규) | $89 1회 | 관리자 1명 + `email-only` 스코프 시트 5개 포함(대상: billing@/support@를 팀원과 나누는 소규모 팀) |
| 추가 시트 | $12 1회/명 | Team 5개 초과 시, 초대 즉시 발급(Stripe 1회 결제 → 초대 링크) |
| 시트 회수 | 무료, 무제한 | 팀원 퇴사 시 즉시 비활성화 가능해야 함(반대로 이건 반드시 무료·무제한이어야 신뢰가 생김) |

이렇게 하면:

- **"구독 없음"이라는 핵심 약속을 지킨다** — 마케팅 카피("$39, one-time, no subscriptions")를 훼손하지 않는다.
- **경쟁사 대비 파괴적인 가격 포지션**을 만든다: Help Scout/Front는 팀 5명이면 월 $100~250(연 $1,200~3,000). Relaybase Team은 그 금액을 평생 1회 $89~149 사이로 대체한다. 이 격차 자체가 웹사이트 카피/비교 표(`pricing-comparison.tsx`)에 넣을 만한 핵심 메시지다.
- **업셀 경로가 명확**해진다: Solo → Team은 "팀원이 생겼을 때" 자연스러운 업그레이드 트리거이므로 강매로 느껴지지 않는다.

### 3.4 나중에 recurring을 고려해도 되는 지점 (지금은 아님)

시트 초대/회수 자체는 절대 구독으로 만들지 않는 것을 권장하지만, **Relaybase가 실제로 서버 비용을 지는 부가 기능**(예: 팀 인박스의 SLA/할당(assignment) 알림을 Relaybase가 운영하는 푸시 서버로 처리, 또는 팀 간 검색 인덱싱을 Relaybase가 대신 호스팅하는 경우)이 생기면 그 기능만 선택적 월 구독으로 분리하는 것은 모델과 충돌하지 않는다 — "메일 자체는 항상 당신 계정에서, 당신 소유"라는 원칙을 지키는 한, 그 위에 얹는 편의 기능은 별도로 과금해도 브랜드가 깨지지 않는다. 지금 단계에서는 이 갈래를 열어두는 정도로만 언급한다.

### 3.5 필요한 엔지니어링 (우선순위)

1. `server/src/lib/keys.ts` 패턴을 확장해 `mailbox`+`role` 스코프 시트 레코드/발급/검증 API 추가
2. `app/` 대시보드에 "역할" 개념 도입 — `role: "email-only"`일 때 Domains/Accounts/API Keys/Settings 내비게이션 숨김, 인박스/컴포즈만 노출
3. Owner 쪽 UI: Settings에 "Team" 섹션 — 초대 링크 생성(이메일 지정 + 대상 mailbox 지정), 시트 목록, 즉시 회수
4. Stripe: Team SKU + 추가 시트 1회 결제 체크아웃, 결제 성공 시 시트 활성화 웹훅(기존 `license.ts`의 `stripe-webhook` 패턴 재사용 가능)

---

## 4. 통합 SKU 표 (전체 그림)

| SKU | 가격 | 범위 | 대상 |
|---|---|---|---|
| Solo | $39 1회 | 1 Worker, 관리자 1명, 무제한 개인 기기(Mac/Win/모바일, 소프트 캡 5) | 1인 빌더 |
| Team | $89 1회 | Solo 전부 + `email-only` 시트 5개 | 소규모 팀, 공유 support@/billing@ |
| 추가 시트 | $12 1회/명 | Team 초과분 | 팀 성장 |
| Studio | $99 1회(또는 3-pack 할인) | Worker 설치 3개 묶음 | 에이전시/멀티 프로덕트 운영자 |
| 추가 Worker 설치 | $29 1회/개 | Studio 초과분 | 신규 사업체 추가 |
| 모바일 컴패니언 | 무료(라이선스 보유자) | 앱스토어 무료 배포, 다운로드 자체가 데스크톱 퍼널 | 발견성/입소문 |

전체적으로 **"평생 구독료 0원"이라는 한 문장을 어떤 SKU에서도 깨지 않는 것**이 Relaybase가 Google Workspace·Front·Missive를 상대로 가진 유일하고 방어 가능한 무기다. 세 질문에 대한 답 모두 이 원칙에서 벗어나지 않도록 설계했다.

## 5. 리스크 및 열린 질문

- Worker 오픈소스화는 법무 검토 없이 라이선스 텍스트를 그대로 채택하지 말 것(BSL/FSL 문구는 회사명/제품명 치환이 필요).
- 기기 활성화 소프트 캡 도입은 기존 구매자(캡 없음)에게 소급 적용되면 반발 소지 — 신규 라이선스부터 적용하거나 기존 고객은 무제한 유지로 그랜드파더링 권장.
- Team/시트 기능은 지금 코드베이스에 전혀 없는 신규 기능이므로, 가격표를 웹사이트에 올리기 전에 최소 MVP(초대 발급 + 역할 필터 UI)가 먼저 필요하다.
- Studio(멀티 Worker) SKU의 라이선스 검증은 "Worker 설치 개수"를 실제로 계산할 방법이 필요 — 2.3의 `workerUrlHash` 활성화 로그를 그대로 재사용해 "이 라이선스에 연결된 고유 Worker 수"를 집계하면 별도 인프라 없이 구현 가능.

---

## 6. "처음엔 일회성으로 팔고 나중에 구독으로 전환"은 괜찮은 전략인가 — Screen Studio 사례

### 6.1 실제로 있었던 일

[Screen Studio](https://screenstudio.net)(macOS 화면 녹화 앱)는 한때 최대 $229짜리 **평생 일회성 라이선스**를 팔았다. **2025년 10월, 신규 구매자 대상으로 이 옵션을 완전히 제거**하고 $29/월 또는 $108/년($9/월 상당) **구독 전용**으로 전환했다. 기존 라이선스 보유자는 그랜드파더링되어 계속 무료로 쓸 수 있었다(이 문서 작성 시점 기준 사용자 본인도 그 그랜드파더링 대상 — 그래서 개인적으로는 영향이 없었다는 경험이 이 질문의 배경이다).

창업자는 공개적으로 "평생 라이선스 티어는 실수였다, 채택률이 낮았다"고 밝혔고, 부트스트랩 팀 입장에서 "할인된 일회성 판매보다 반복 매출이 필요했다"는 취지로 설명했다. 결과:

- Hacker News, Reddit r/macapps에서 상당한 반발("이건 결국 얼마나 참을 수 있는지 시험하는 것", "데스크톱에 도는 앱에 구독이라니 너무한다")
- "구독 없는 대안"을 정면으로 마케팅 포인트로 삼는 경쟁 제품(ScreenKite, CursorClip 등)이 이 반발을 그대로 흡수하며 등장
- 신규 구매자 기준 환불 불가·프로모션 없음으로 조건이 더 단단해짐

즉 이 전환은 **회사 입장에서는 매출 예측성을 확보했지만, 브랜드 신뢰와 경쟁 포지션에는 실질적 비용을 치른 사례**다 — "성공"과 "실패" 중 하나로 딱 잘라 말할 수 없는, 트레이드오프가 분명한 선례다.

### 6.2 Relaybase에 그대로 적용하기 전에 짚어야 할 구조적 차이

| | Screen Studio | Relaybase |
|---|---|---|
| 구독 반대가 핵심 마케팅 축이었나 | 아니다 — 화면 녹화 도구일 뿐, "구독 없음"이 판매 이유는 아니었다 | **그렇다** — "$39 일회성, Google Workspace/Front처럼 매월 내지 않는다"가 존재 이유 자체(`README.md`, `PRODUCT.md`) |
| 사용 패턴 | 버스티(데모 찍을 때만 씀) → 안 쓰는 달엔 구독을 일시정지할 수 있다는 게 일부 사용자의 방어 논리였음 | **상시 인프라** — 매일 들어오고 나가는 실제 업무 이메일. "이번 달엔 구독 끄고 안 씀" 자체가 불가능(끄면 이메일이 죽는다) |
| COGS 서사 | 자체 서버 비용 거의 없음에도 "반복 매출이 필요하다"는 사업적 이유만으로 전환 | 마찬가지로 서버 비용이 사용자 계정에 있어 0에 가깝다는 게 **가격 경쟁력의 근거**로 이미 쓰이고 있음 — 이 위에 강제 구독을 얹으면 자기 논리와 정면 충돌 |

세 가지 차이 모두 같은 방향을 가리킨다 — **Relaybase가 같은 방식(코어 기능을 신규 구매자부터 구독 전용으로 바꾸는 것)으로 전환하면, Screen Studio보다 반발이 더 크고 더 정당한 반발일 가능성이 높다.** Screen Studio 사용자는 "쓰기 싫으면 그 달만 끄면 된다"는 도피처가 있었지만, Relaybase 사용자는 이메일이 살아있는 한 끌 수 없다 — 이건 "가끔 쓰는 도구의 구독"이 아니라 "인질로 잡힌 인프라의 구독"으로 읽힐 위험이 있다.

### 6.3 그럼에도 "여지"는 남겨둘 수 있다 — 단, Screen Studio와는 다른 방식으로

일회성으로 시작해서 나중에 반복 매출을 추가하는 것 자체는 방향이 맞다(유지보수비가 반복적이라는 2번 질문의 결론과 동일). 다만 **"코어 상품을 나중에 구독으로 바꿔치기"가 아니라 "코어는 절대 건드리지 않고 그 위에 옵션을 얹는"** 방식이어야 Screen Studio식 반발을 피할 수 있다.

1. **그랜드파더 약속을 지금, 판매 전에 공개적으로 선언한다.** Screen Studio의 반발이 컸던 이유 중 하나는 "약속이 나중에 바뀔 수 있다"는 인상 자체였다. Relaybase는 반대로 지금 웹사이트/이용약관에 "$39/Team/Studio로 구매한 코어 라이선스는 향후에도 절대 구독으로 전환되지 않는다"를 **선제적으로 명문화**해서 이걸 오히려 오늘의 판매 포인트로 쓴다 — "우리는 Screen Studio가 한 것을 하지 않겠다"고 직접 말할 수 있는 셈이다.
2. **신규 구매자에게도 일회성 옵션 자체를 절대 없애지 않는다.** Screen Studio의 결정적 실수는 기존 고객을 그랜드파더링한 것과 별개로 **신규 고객이 일회성으로 살 수 있는 문(門) 자체를 닫아버린 것**이다. Relaybase는 이 문을 영구히 열어둔다 — Solo/Team/Studio(4번 섹션의 SKU)는 신규 고객에게도 계속 일회성으로 팔리고, Care Plan 같은 반복 매출 레이어는 "코어를 대체"하는 게 아니라 "코어 위에 얹는 선택 사항"으로만 존재한다.
3. **가격을 지금부터 넉넉하게 책정해 애초에 전환 압박 자체를 줄인다.** 2번 섹션의 SKU 가격($39/$89/$99)이 유지보수비를 못 커버할 정도로 낮게 잡히면, 미래의 "우리도 결국 구독으로 가야 하나"라는 압박이 커진다. 처음부터 유지보수비 마진을 감안한 가격($59~79 기준가 검토, 앞선 캔버스 분석 참고)을 잡아두면 Screen Studio가 겪은 "저가 일회성 → 뒤늦은 강제 전환"의 궤적 자체를 밟지 않을 수 있다.

### 6.4 결론 (v1 관점 — 7번 섹션에서 전제 자체를 재검토함)

"일회성으로 시작해서 나중에 구독을 더하는 것"은 전략적으로 괜찮다 — 실제로 이 문서의 2·3번 섹션(Care Plan, Team 시트)이 이미 그 방향이다. 문제가 되는 것은 **"나중에 코어 상품 자체를 구독으로 바꿔치기"**이며, 이건 Relaybase의 경우 Screen Studio보다 훨씬 위험하다(브랜드 약속이 정면으로 깨지고, 사용자는 도피처 없이 갇힌 인프라 구독을 강요받는 셈이 되기 때문). 따라서: **코어는 영구 일회성으로 못박고, 반복 매출은 항상 "선택적 애드온"으로만 설계한다**는 원칙을 지금 문서에 명시해 둔다.

다만 위 결론은 "$39, 구독 없음"이라는 v1 카피를 지켜야 한다는 전제를 깔고 도출된 것이다. 아직 제품이 공개되지 않았고, 그 카피 자체가 검증된 시장 반응이 아니라 초안일 뿐이라면 — 전제를 내려놓고 처음부터 다시 계산해야 한다. 아래 7번 섹션이 그 작업이다.

---

## 7. 가격체계 재설계 — 마케팅 카피와 독립적인 객관적 선택 (v2)

이 섹션은 "구독 없음이 옳다"를 방어하는 게 아니라, 5개 가격 모델을 동일한 기준으로 채점해서 비교한다. 상세 시나리오·차트는 [relaybase-pricing-arr-model](/Users/isaaclee/.cursor/projects/Users-isaaclee-Projects-productions-relaybase/canvases/relaybase-pricing-arr-model.canvas.tsx) 캔버스에서 시나리오를 바꿔가며 확인할 수 있다(이 문서의 숫자와 동일한 모델).

### 7.1 평가 기준

| 기준 | 의미 |
|---|---|
| ARR 실현성 | 신규 판매가 멈춰도 남는 반복 매출이 실제로 생기는가 |
| 신뢰/이탈 리스크 안전성 | 나중에 바꾸거나 약속을 못 지킬 때 브랜드가 받는 피해 크기 |
| 아키텍처 적합성 | Relaybase의 "Worker는 사용자 계정에서 독립적으로 실행"이라는 구조와 맞는가 |
| 퍼널 도달력 | SOM(연 1.5만 명)이라는 작은 니치에서 발견/전환 마찰을 줄이는가 |
| 구현 난이도 | 지금 코드베이스 기준 추가 엔지니어링 규모 |

### 7.2 5개 모델 비교

| 모델 | ARR 실현성 | 신뢰 리스크 안전성 | 아키텍처 적합성 | 퍼널 도달력 | 구현 난이도 |
|---|---|---|---|---|---|
| 순수 일회성 (v1 초안, $39~79 단품) | 없음 | 매우 높음 | 매우 높음 | 낮음 | 낮음(이미 구현) |
| 순수 구독 (Screen Studio식 전환) | 높음 | 매우 낮음 | 낮음 — Worker에 라이선스 게이팅 추가 필요, 메일 서비스 인질화 리스크 | 중간 | 중간 |
| 이전 하이브리드 (일회성 + 옵트인 Care Plan) | 낮음~중간 | 매우 높음 | 매우 높음 | 낮음 | 낮음 |
| **신규 추천: Free + Pro 일회성 + 기본프레임 연간 갱신** | 중간~높음, 매년 복리 | 매우 높음 — perpetual fallback을 아키텍처로 증명 가능 | 매우 높음 — Free 티어도 사용자 Worker라 COGS 0 | 매우 높음 — 무료 티어가 진입장벽 제거 | 중간 |
| 사용량 기반 (주소/발송량 종량제) | 중간 | 중간 — 사용량 텔레메트리가 "데이터 안 본다" 약속과 마찰 | 낮음 — Worker가 사용량을 리포트해야 함 | 중간 | 매우 높음 |

**객관적 근거 (실제 사용자 발언, Screen Studio HN 스레드)**: *"I bought it when it was a flat fee for a year of updates. That year's up now and I'd happily pay for another, but not as a subscription."* — 구독에 알레르기가 있는 정확히 이 타깃 구매자층도 "1년 업데이트에 대한 정액 갱신"은 기꺼이 다시 낸다. "열려있는 구독"과 "1년 업데이트권 갱신"은 같은 금액이어도 심리적으로 다른 상품이라는 뜻이며, 신규 추천 모델이 정확히 이 구조다.

### 7.3 추천 모델: Free + Pro 일회성 + 기본프레임 연간 업데이트 갱신

핵심 메커니즘: Pro를 구매하면 앱 + Worker는 **영구히** 동작한다(perpetual fallback). 연 $25 갱신은 "계속 쓰기 위한 구독료"가 아니라 "새 기능·보안 패치를 계속 받기 위한 선택"이다 — 이메일 송수신 자체는 사용자 자신의 Worker가 독립적으로 처리하므로 갱신을 안 해도 멈추지 않는다. 이 한 가지 아키텍처적 사실이 Screen Studio식 반발(6번 섹션)을 구조적으로 피하게 해준다: Screen Studio는 구독이 끊기면 핵심 기능(내보내기)이 막히지만, Relaybase는 갱신이 끊겨도 핵심 기능(메일 송수신)이 막히지 않는다.

| SKU | 가격 | 내용 |
|---|---|---|
| Free | $0 | 1 Worker, 1 도메인, 1 이메일 주소, 송수신, 커뮤니티 문서 |
| Pro | $69 (일회성) | 무제한 도메인/주소, Audience/Broadcasts/Metrics, Mac+Windows+모바일, 팀 시트 3개 포함, 업데이트 1년 포함 |
| Pro 업데이트 갱신 | $25/년 (선택) | 신규 기능·보안/API 대응 패치 계속 수신. 갱신 안 해도 앱·이메일은 마지막 버전으로 영구 동작, 나중에 다시 갱신해도 불이익 없음 |
| 추가 팀 시트 | $15 (일회성/명) | 이메일 전용 스코프 시트, 3개 초과분 |
| Studio | $149 (일회성) | Worker 설치 3개 묶음 — 에이전시/멀티 프로덕트 운영자 |
| 추가 Worker 설치 | $50 (일회성/개) | Studio 초과분 |

Free 티어는 사용자 자신의 Cloudflare Worker에서 실행되므로 Relaybase의 COGS는 0에 가깝다 — 일반적인 SaaS의 "무료 사용자가 비용을 유발한다"는 전제가 Relaybase에는 적용되지 않는다는 구조적 특성을 그대로 이용한 것이다.

### 7.4 v1 대비 시뮬레이션 결과 (3년차, 캔버스 기준)

| 시나리오 | v1 3년차 총매출 | v2 3년차 총매출 | 매출 증가 | v1 순마진 | v2 순마진 | 마진 증가 |
|---|---|---|---|---|---|---|
| 보수적 | ~$17,330 | ~$23,328 | +35% | ~$5,211 | ~$11,029 | **+112%** |
| 기준 | ~$44,846 | ~$60,649 | +35% | ~$31,902 | ~$47,231 | +48% |
| 낙관적 | ~$99,990 | ~$135,556 | +36% | ~$85,391 | ~$119,890 | +40% |

가장 위험했던 **보수적 시나리오에서 마진이 2배 이상 개선**된다는 점이 핵심이다 — 애초에 이 질문("수요가 많은 제품이 아니라서 걱정된다")이 나온 지점을 정확히 보강한다. 세 시나리오 모두에서 매출 자체도 약 35% 높다(가격 인상 $39→$69에도 완만한 탄력성 가정 하에 순증가).

### 7.5 이 가격체계에 맞춘 마케팅 카피 방향

카피를 먼저 정하고 가격을 거기 맞추는 게 아니라, 위에서 고른 가격체계(7.3)에 맞춰 카피를 새로 쓴다.

- **헤드라인 후보**: "1개 도메인은 무료. 다 쓰려면 한 번만 결제하세요." / "당신의 이메일은 절대 멈추지 않습니다 — 당신의 Cloudflare 계정 안에서, 당신 소유로 실행되기 때문입니다."
- **신뢰 메시지 — 약속이 아니라 메커니즘으로 증명**: FAQ "구독을 안 내면 어떻게 되나요?"에 "앱과 이메일은 마지막 버전으로 계속 동작합니다 — Worker가 당신의 Cloudflare 계정에서 독립적으로 돌기 때문입니다. 저희가 서버를 내려도 당신 메일은 멈추지 않습니다." Screen Studio 사례처럼 "약속"에 기대지 않고, 검증 가능한 아키텍처 사실만 카피에 쓴다.
- **경쟁 비교 재구성**: "구독 없음 vs 구독"이라는 이분법 대신 숫자로: Google Workspace $84/년/좌석, Front/Missive $240~600/년/좌석 대비 Relaybase는 평생 한 번 $69 + (원한다면) 연 $25 — 그마저 안 내도 이메일은 안 멈춘다.
- **1~6번 섹션에서 쓴 "구독료 0원" 절대 문구는 폐기**하고, "강제 구독 없음 + 원하면 업데이트 갱신"으로 교체한다. Worker 오픈소스화(1번 섹션), 라이선스=Worker 설치 단위 스코프(2번 섹션), 이메일 전용 시트(3번 섹션)의 결론은 그대로 유효하다.

### 7.6 결론

가격체계는 카피가 아니라 지표로 정한다: 5개 모델 중 **Free + Pro 일회성($69) + 기본프레임 연간 업데이트 갱신($25, perpetual fallback)** 이 ARR 실현성·신뢰 안전성·아키텍처 적합성·퍼널 도달력에서 가장 높은 점수를 받고, 세 시나리오 모두에서 매출과(특히) 마진을 개선한다. "구독을 절대 안 한다"는 문장은 폐기하되, "구독을 강제하지 않는다 + 안 내도 안 멈춘다"는 더 구체적이고 아키텍처로 증명 가능한 약속으로 교체해 카피를 새로 쓴다.

---

## 8. Cloudflare Email Sending이 아직 공개 베타인 상황에서 prelaunch 타이밍 (v1)

### 8.1 현재 상태 (근거)

- Relaybase의 발신 경로 전체가 Cloudflare의 **Email Sending REST API** 한 엔드포인트에 물려 있다 — 대체 발신 경로가 없다.

```227:230:server/src/lib/cloudflare-client.ts
    if (replyTo) body.reply_to = replyTo;

    const path = `/accounts/${this.accountId}/email/sending/send`;
    const data = await this.sendWithRetry<{
```

- 이 API는 **2026-04-16 "public beta" 발표** 기준으로도 여전히 베타다(그 이전엔 2025-11경부터 private beta). 공개 문서·블로그 어디에도 GA(정식 출시) 일정이 명시되어 있지 않다. Cloudflare 공식 확인: *"Email Sending is now in public beta... Start sending and receiving emails from Workers and agents today."* (2026-04-16 changelog), *"Email Sending graduates from private beta to public beta today."* (Cloudflare Blog, 같은 날).
- 코드베이스는 이미 이 베타의 거친 모서리를 상당히 인지하고 대응해 두었다 — "도메인이 아직 onboarding 안 됨", "not_entitled", "sending_disabled" 같은 베타 특유의 에러코드를 사람이 읽을 수 있는 힌트로 변환하는 로직이 이미 존재한다:

```9:15:server/src/lib/cloudflare-api-hints.ts
  if (p.includes("/email/sending/send")) {
    return [
      `Endpoint: ${m} /accounts/{{account_id}}/email/sending/send`,
      "Required: Account → Email Sending → Edit",
      "The From domain must be onboarded in Cloudflare → Email Service → Email Sending.",
      "Before onboarding, you can only send to verified destination addresses.",
    ].join("\n");
  }
```

- 이는 "도메인 연결 즉시 발신 가능"이라는 온보딩 UX 약속에 실제 제약이 걸려 있다는 뜻이다: 도메인이 Cloudflare 쪽에서 **onboarding 완료 전에는 검증된 수신 주소로만** 발신이 가능하다. 이 지연/승인 절차가 사용자에게 얼마나 걸리는지는 Relaybase가 통제할 수 없는 영역이다.
- 가격 페이지(Cloudflare Email Service Pricing, 2026-06-09 갱신)에 따르면 **발신은 사용자의 Cloudflare 계정이 Workers Paid 플랜(월 $5)일 때만 가능**하고, 월 3,000건 무료 포함 후 1,000건당 $0.35다. 이는 아직 이 저장소 어디에도 명시적으로 반영되어 있지 않다 — 특히 7.3의 **Free SKU($0, "송수신" 포함)**는 사실상 "Relaybase에는 $0이지만 Cloudflare에는 최소 $5/월"이라는 전제를 깔고 있는데, 이 전제가 마케팅 카피나 온보딩 화면에 아직 드러나지 않는다.

### 8.2 과거 사례로 본 베타 기간의 현실적 길이

Cloudflare Email Routing(자매 제품, 지금 `server/`의 수신 경로가 그대로 쓰는 API)의 실제 타임라인:

| 단계 | 시점 |
|---|---|
| 발표 + closed beta(waitlist) | 2021-09 |
| open beta(누구나 가입 가능) | 2022-02-08 |
| **GA(베타 종료)** | 2022-10-25 |

즉 "waitlist 없는 오픈 베타"에서 GA까지 **약 8.5개월**이 걸렸고, 최초 발표부터는 **13개월**이었다. Email Sending은 이미 2025-11경 private beta → 2026-04-16 public beta로 넘어왔으므로 같은 패턴을 적용하면 **GA는 대략 2026년 4분기~2027년 상반기** 사이가 현실적인 기대치다 — 그리고 이건 "이메일 라우팅(수신)"보다 스팸/신뢰도/발신자 평판 관리가 훨씬 더 어려운 "발신" 제품이라는 점을 고려하면 오히려 더 길어질 수도 있다.

**결론: GA를 기다렸다가 launch하는 전략은 사실상 "6개월~1년 이상 통째로 대기"를 의미한다.** SOM이 연 1.5만 명 수준인 니치 제품에서 이 정도 대기는 감당하기 어렵다.

### 8.3 핵심 원칙 — "prelaunch"와 "유료 정식 launch"를 분리한다

베타 종료를 기다릴 필요가 없는 이유는 이 둘이 지는 리스크가 완전히 다르기 때문이다:

| | 리스크 성격 | CF 베타 상태에 대한 노출 |
|---|---|---|
| **Prelaunch**(랜딩페이지, 웨이트리스트, HN/PH 예고, 콘텐츠 마케팅) | 관심 수집일 뿐, 실제 이메일이 오가지 않음 | **노출 없음** — 지금 당장 해도 무방 |
| **유료 정식 launch**(라이선스 판매, 실사용자가 실제 업무 이메일을 이 경로로 발신) | CF API 브레이킹 체인지·정지·가격 변경이 실사용자 이메일에 직접 영향 | **노출 큼** — 타이밍 판단이 필요한 지점 |

즉 지금 고민해야 할 건 "prelaunch를 언제 하느냐"가 아니라 이미 답이 나와 있다 — **지금 해도 된다.** 실제로 판단이 필요한 지점은 "웨이트리스트를 실제 결제 고객으로 전환(=정식 launch)하는 시점"이다.

### 8.4 오히려 지금 "베타"라는 사실 자체가 프리런치 카피의 자산이 될 수 있다

1번 섹션에서 이미 세운 신뢰 서사("Worker는 오픈소스, 검증은 당신이 한다", "We do not host your mail")와 정확히 같은 논리로, **Cloudflare Email Sending이 베타라는 사실을 숨기지 않고 먼저 말하는 것**이 오히려 타깃 고객(Cloudflare를 이미 쓰는 개발자층)에게는 신뢰 신호다. 이 페르소나는 베타 API를 다루는 데 익숙하고, 오히려 "너네도 아는구나, 숨기지 않는구나"에 더 반응한다.

- 프리런치 랜딩페이지/웨이트리스트 카피에 명시: *"Built on Cloudflare's new Email Sending API (currently in public beta). We track every change and ship Worker updates the same day — you'll always know exactly what's happening under the hood."*
- 이렇게 프레이밍하면 정식 launch를 늦출 이유가 아니라, **초기 가입자를 "베타를 함께 타는 얼리어답터"로 셀프 셀렉션**시키는 장치가 된다 — Screen Studio 사례(6번 섹션)에서 배운 "약속을 지금 명문화한다"는 원리를 여기에도 적용하는 것.

### 8.5 유료 정식 launch를 판단하는 기준 (GA를 기다리지 않는 대신 쓸 체크리스트)

GA 공지를 기다리는 대신, 아래 4개 신호를 **내부 게이트**로 삼는다. 전부 충족될 필요는 없지만 최소 1·2번은 필수:

1. **자기 도메인(관리자용 relaybase.xyz 등)에서 최소 4~6주 실사용(dogfooding)** — 실제 라이선스 키 발송/알림 메일을 이 발신 경로로 돌리면서 breaking change·지연·거부율을 직접 관찰. 이미 `admin/`이 자체 도메인 발신에 이 경로를 쓰고 있다면 이 기간은 이미 누적되고 있을 수 있음 — 실제 누적 기간을 먼저 확인.
2. **얼리액세스 코호트(5~20명, 웨이트리스트에서 선발) 4주 무료/할인 트라이얼** — 결제를 받기 전에 실제 제3자 도메인 onboarding 지연시간, 발신 성공률을 계측.
3. **Cloudflare Email Service changelog에 60일 연속 breaking change 없음** — RSS(`developers.cloudflare.com/changelog/product/email-service/`)를 구독해 추적. 가격 변경 공지가 있었는지도 함께 확인(이미 6/9 가격 페이지 갱신이 있었으므로 가격 자체는 어느 정도 안정화된 신호로 볼 수 있음).
4. **(이상적, 필수 아님) Cloudflare의 공식 GA 발표.**

**권장 결론: 1·2번이 확보되면(대략 6~8주 내 가능) 유료 정식 launch를 진행하되, 카피에 8.4의 "베타 투명 공개" 문구를 유지하고 결제 페이지에 "Early Access" 배지를 단다.** GA는 몇 달 뒤에 "이제 정식 GA — Early Access 배지 제거" 이벤트로 재활용한다(2차 마케팅 모멘트).

### 8.6 지금 코드/문서에서 같이 손봐야 할 갭

- `PRODUCT.md`/웹사이트 Free 티어 카피에 **"발신에는 사용자의 Cloudflare 계정이 Workers Paid 플랜(월 $5, Cloudflare에 직접 결제)이 필요"**를 명시해야 한다 — 지금처럼 안 밝히면 "Free인데 왜 돈이 나가냐"는 온보딩 중 신뢰 손상 지점이 된다. Solo/Pro도 마찬가지로 명시가 필요.
- 온보딩 UX 카피("몇 분 안에 연결 완료")에 **도메인 onboarding 승인/검증 지연 가능성**을 완화된 문구로 반영("보통 몇 분, Cloudflare 쪽 승인에 따라 다소 걸릴 수 있음") — `cloudflare-api-hints.ts`가 이미 이 제약을 알고 있으므로 UI 카피만 맞추면 됨.
- `admin/` 대시보드에 Cloudflare Email Service changelog 변경 감지(수동 체크 or RSS 폴링)를 얹어 8.5의 3번 체크를 습관화.

### 8.7 결론

베타 종료(GA)를 prelaunch의 전제조건으로 삼지 않는다 — 과거 사례(Email Routing) 기준 GA는 6개월~1년 뒤일 수 있고, 이만큼 기다리는 건 이 니치 시장에서 감당할 수 없는 지연이다. 대신: **(1) 랜딩페이지/웨이트리스트 prelaunch는 지금 즉시 시작**하고, 카피에서 베타 의존성을 투명하게 먼저 밝혀 신뢰 자산으로 전환한다. **(2) 유료 정식 launch는 GA가 아니라 자체 dogfooding + 소규모 얼리액세스 코호트 결과(6~8주)를 게이트로 삼아 "Early Access" 배지와 함께 진행**하고, 실제 GA 발표를 나중에 배지를 떼는 2차 마케팅 이벤트로 재사용한다. 그 사이에 Free/Solo/Pro 카피에 "발신에는 사용자 쪽 Cloudflare Workers Paid 플랜이 필요하다"는 사실을 지금 명시해 신뢰 손상 지점을 미리 없앤다.
