{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "biometry",
  "description": "Touch ID (macOS) / Windows Hello for owner-session unlock",
  "windows": ["main"],
  "platforms": ["macOS", "windows"],
  "permissions": ["biometry:default"]
}
