{
  "name": "relaybase-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types src/lib/mobile-config.test.ts",
    "pack:customer-install": "node scripts/pack-customer-install.mjs",
    "migrate:kv-prefix": "node scripts/migrate-kv-prefix.mjs",
    "migrate:kv-prefix:apply": "node scripts/migrate-kv-prefix.mjs --apply",
    "import:mbox": "node scripts/import-mbox.mjs",
    "import:mbox:apply": "node scripts/import-mbox.mjs --apply"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "postal-mime": "^2.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250613.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
