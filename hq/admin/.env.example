# Relaybase admin — replace with real values
RELAYBASE_URL=https://relaybase-api.gssisaac.worker.dev
RELAYBASE_CONSOLE_URL=https://console.relaybase.xyz

# Passkey required to access the admin dashboard (see middleware.ts)
# In production, set this as a Worker secret: wrangler secret put ADMIN_PASSKEY
ADMIN_PASSKEY=change-me
