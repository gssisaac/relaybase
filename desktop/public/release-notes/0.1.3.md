---
date: 2026-09-04
---

# Relaybase 0.1.3

Hotfix release. Replaces a bad **0.1.2** CDN object that served an older build under the same filename.

## Highlights
- Correct desktop build with Settings → Update and install-success fixes
- New download filenames (`0.1.3`) so poisoned `0.1.2` CDN cache cannot be reused

## Changes
- Ship the intended 0.1.2 desktop changes under patch **0.1.3**
- Worker install bundle stays **0.1.1** — no Worker script changes
