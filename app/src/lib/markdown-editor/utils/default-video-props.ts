/** Default props for embedded video blocks (YouTube and file videos). */
export const DEFAULT_EMBEDDED_VIDEO_PROPS = {
  showPreview: true,
  previewWidth: 560,
  textAlignment: "left" as const,
  backgroundColor: "default" as const,
  caption: "" as const,
  name: "" as const,
};
