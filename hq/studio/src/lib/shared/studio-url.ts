/** Public links in outbound mail (unsubscribe, open/click, assets). Served via relaybase.email → hq/studio. */
export const STUDIO_PUBLIC_BASE_URL =
  process.env.STUDIO_PUBLIC_BASE_URL ?? "https://relaybase.email";
